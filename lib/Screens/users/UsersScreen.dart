import 'package:flutter/cupertino.dart';

class usersScreen extends StatelessWidget {
  const usersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('users'),);
  }
}
